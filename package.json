{
  "name": "ynab-keys",
  "version": "0.0.1",
  "main": "index.js",
  "author": "karl@karltaylor.dev",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c && cp manifest.json ./dist",
    "ts": "yarn tsc && cp manifest.json ./dist && cp background.html ./dist"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^9.0.1",
    "rollup": "^3.1.0",
    "rollup-plugin-terser": "^7.0.2",
    "tslib": "^2.4.0",
    "typescript": "^4.8.4"
  }
}
